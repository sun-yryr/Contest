#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cmath>
#include <algorithm>
#include <vector>
#include <stack>
using namespace std;
#define REP(i, n) for(int (i)=0; (i)<(n); (i)++)
#define OUT(a) cout << (a) << "\n"
#define ll long long
ll MOD = 10e9 + 7;

int main() {
    int n,k;
    cin >> n >> k;
    vector<int> v;
    int tmp;
    REP(i, n) {
        scanf("%d ", %tmp);
        v.push_back(tmp);
    }
    int left_item, right_item;
    while(k >= 0) {
        
    }
    return 0;
}